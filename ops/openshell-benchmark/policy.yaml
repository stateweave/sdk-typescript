version: 1

filesystem_policy:
  include_workdir: true
  read_only:
    - /usr
    - /etc
    - /proc/self
  read_write:
    - /sandbox
    - /tmp

landlock:
  compatibility: hard_requirement

process:
  run_as_user: sandbox
  run_as_group: sandbox

network_policies: {}
