# Jev focus diagnostic v2 — freeze before any v2 provider calls

This supersedes an **invalid, stopped calibration**, not a negative or positive efficacy result. V1 requested user JSON with an `answer` property, which the public SDK correctly interprets as its FINAL envelope and unwraps. Its scorer then attempted to parse the already-unwrapped answer as JSON, mechanically rejecting legitimate answers. The run was stopped for this instrumentation defect; every completed record and interrupted start is retained. Do not count v1 scores as evidence for or against Jev.

V1 also revealed public Agent rejection of the common unambiguous `TOOL_CALL:` envelope and exhausted 1024-token output calls. The public Agent now accepts an optional colon at the anchored action boundary, without changing the frozen runner parser. V2 uses Dev's actual 4096 output ceiling equally across arms. Hierarchy descriptors now prefer substantive semantic/resource/tool/verification excerpts over bookkeeping goals and `Recorded` acknowledgments; this is a disclosed treatment revision.

## Fixed diagnostic

Reuse **all twelve** original cases, without outcome-based selection or edits. The fixture remains `../jev-focus-v1/cases.json`, SHA-256 `1383bb9de5b180598ea2e76d9e0e275129f5c7a3f4f3890dbfa0015d1d9d02e4`. This is a diagnostic replay on a reused designed corpus, **not independent confirmation**. No significance or generalization claim is permitted from it.

All three arms use the same new SDK commit, model `glm-5.3-flash`, temperature 0, output ceiling 4096, tool definitions, initial immutable 217-node states, read_file contents, five iterations, 120-second wall deadline, 16-node molecular selection, 16K soft context target and 64K hard ceiling. Jev remains pinned to `jev-1.13.0`. Mode, branch/candidate bounds, global bypass, thresholds and timeout policies are unchanged from v1. Rotate arm order by case index; no parallel evaluation calls. No retries or optional stopping. No source/runtime updates while this trajectory runs.

The task now asks for ordinary final-answer text containing the requested decision or values and the live file marker. Score `AgentRunResult.finalAnswer` directly. Full pass requires every case-insensitive expected substring, absence of all forbidden stale values, the exact probe marker, a native read_file call, its actual tool-result node in the final compiled read set, all required current semantic evidence nodes in that read set, preserved starting graph, exact answer-parent read-set provenance, and matching context ceilings. Report answer correctness separately from this strict full-pass endpoint. UNKNOWN's empty evidence set has recall 1 by convention.

Publish all twelve paired rows, errors and actual stage/candidate diagnostics. Include failed attempts in totals. A protocol/recursion/tool failure scores zero; a clearly external model-provider outage makes the entire paired case operationally incomplete rather than a win for its peers. Sidecar fallback is delivered behavior and remains scored; completed stage usage is retained and missing usage marked incomplete. Keep attempted `.started.json` records and never silently replay unfinished calls.

Report descriptive full-pass/answer counts, required-evidence recall, main-model and Jev token totals separately and together, complete call counts, fallback rate, median complete-task latency and median Jev overhead. No dollar-price claim without verified dated provider prices. Primary software tests and Dev HTTP/browser/session smokes remain separate plumbing evidence. A future efficacy claim requires a new independent corpus and defensible power plan. Feature stays opt-in regardless of outcome.
